# Keep environment secrets local
.env
.env.local
.env.*.local

.langgraph_api/

# Build artifacts from the pyproject.toml manifest. Regenerated by any
# `pip install .`, which is what `langgraph dev` runs for `"dependencies": ["."]`.
*.egg-info/
build/
dist/
